#include <stdio.h>

/**
 * main - a C program that prints a phrase
 *
 * Return: 0 if exited properly, non-zero otherwise
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
