#include <stdio.h>

/**
 * main - main block
 * Description: prints all single decimals
 * Return: 0.
 */

int main(void)
{
	int a = 0;

	while (a <= 9)
		printf("%d", a++);

	printf("\n");
	return (0);
}
